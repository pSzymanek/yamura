---
import { site } from "../../data/site";
import SectionHeading from "../ui/SectionHeading.astro";
import MapCard from "../ui/MapCard.astro";
import SocialLinks from "../ui/SocialLinks.astro";
---

<section class="contact section-light" id="kontakt">
  <div class="section-inner">
    <div class="contact__grid">
      <div class="contact__details" data-reveal>
        <SectionHeading eyebrow="Kontakt" title="Porozmawiajmy o Twojej realizacji." align="left" />
        <dl>
          <div>
            <dt>Zamówienia</dt>
            <dd><a href={`mailto:${site.ordersEmail}`}>{site.ordersEmail}</a></dd>
          </div>
          <div>
            <dt>Telefon</dt>
            <dd><a href={`tel:${site.phoneHref}`}>{site.phone}</a></dd>
          </div>
          <div>
            <dt>Lokalizacja</dt>
            <dd>{site.location}</dd>
          </div>
          <div>
            <dt>Siedziba</dt>
            <dd>{site.registeredAddress.street}<br />{site.registeredAddress.postalCode} {site.registeredAddress.city}</dd>
          </div>
        </dl>
        <div class="contact__social">
          <p class="eyebrow">Znajdź nas</p>
          <SocialLinks />
        </div>
      </div>

      <form class="contact-form" data-contact-form data-recipient={site.ordersEmail} data-reveal>
        <p class="eyebrow">Zapytanie o realizację</p>
        <h2>Opowiedz nam o swojej przestrzeni.</h2>
        <p class="contact-form__intro">Przekaż najważniejsze informacje. Odpowiemy i ustalimy kolejny krok.</p>

        <div class="contact-form__grid">
          <label>
            <span>Imię i nazwisko</span>
            <input type="text" name="name" autocomplete="name" required maxlength="120" />
          </label>
          <label>
            <span>E-mail</span>
            <input type="email" name="email" autocomplete="email" required maxlength="160" />
          </label>
          <label>
            <span>Telefon</span>
            <input type="tel" name="phone" autocomplete="tel" maxlength="40" />
          </label>
          <label>
            <span>Rodzaj realizacji</span>
            <select name="projectType" required>
              <option value="">Wybierz</option>
              <option>Kuchnia</option>
              <option>Salon</option>
              <option>Łazienka</option>
              <option>Biuro</option>
              <option>Inna zabudowa</option>
            </select>
          </label>
          <label class="contact-form__wide">
            <span>Miejsce realizacji</span>
            <input type="text" name="location" autocomplete="address-level2" required maxlength="140" />
          </label>
          <label class="contact-form__wide">
            <span>Wiadomość</span>
            <textarea name="message" rows="6" required minlength="20" maxlength="3000"></textarea>
          </label>
        </div>

        <label class="contact-form__consent">
          <input type="checkbox" name="privacyAccepted" value="true" required />
          <span>Akceptuję przetwarzanie danych w celu odpowiedzi na zapytanie zgodnie z <a href="/polityka-prywatnosci/">polityką prywatności</a>.</span>
        </label>
        <label class="contact-form__trap" aria-hidden="true">
          <span>Strona internetowa</span>
          <input type="text" name="website" tabindex="-1" autocomplete="off" />
        </label>

        <div class="contact-form__actions">
          <button class="btn btn--dark" type="submit" data-contact-submit>Wyślij zapytanie</button>
          <p class="contact-form__status" data-contact-status role="status" aria-live="polite"></p>
        </div>
      </form>
    </div>

    <div class="contact__map" data-reveal>
      <MapCard />
    </div>
  </div>
</section>

<script>
  import "../../scripts/contactForm";
</script>
